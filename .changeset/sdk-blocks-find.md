---
"@cosmicjs/sdk": minor
---

Add `blocks.find()` to fetch a bucket's Rich Text block definitions (`settings.content_blocks`). Pass the result to a rich-text renderer such as `@cosmicjs/rich-text` to expand `{{name /}}` block tokens.
